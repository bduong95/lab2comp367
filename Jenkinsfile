pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                echo "Checkout Step"
            }
        }
        stage('Build') {
            steps {
                echo "Build Step"
            }
        }
        // Add more stages as needed
    }
}
